export interface TrainCardDataType {
  isTrain: boolean;
  trainType: string;
  trainNumber: number;
  departureStation: string;
  arrivalStation: string;
  departureTime: string;
  arrivalTime: string;
}
